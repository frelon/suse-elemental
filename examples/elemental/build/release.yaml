name: suse-product
manifestURI: file://./examples/elemental/build/suse-product-manifest.yaml
# Alternatively, the product release manifest can be specified from an OCI image.
# manifestURI: oci://registry.suse.com/suse-product/release-manifest:0.0.1
components:
  systemd:
    - extension: rke2 # from core release
  helm:
    - chart: metallb # from core release
